# 最近邻来源审计

源域 512 检测器类别 AUROC 排序选出 macaroni2、capsules，各固定取最高分正常图和最低分异常图，共四个事后极端案例。它们用于寻找具体机制线索，不代表错误率或一般类别分布。

现有 memory_bank.pt 没有保存训练图及网格位置索引。此次不重建 coreset：先复现查询图检测分数，再获取原始最大响应网格的最近邻记忆向量，扫描 used_image_ids.json 中原正常训练图，以相同骨干及特征池化恢复来源。

扫描使用原训练 batch size 4。平方距离 shortlist 32 个候选后以直接向量差验证误差。相对嵌入误差不超过 1e-5 才标记来源已验证；未达到时保留最接近候选，但不能称为确切来源。出现重复或近似相同嵌入时，恢复的是一个匹配来源，不能保证是原 coreset 所选的唯一索引。

输出查询与参考原图路径、原始特征网格位置、平滑异常图最高点、记忆库索引及恢复误差。原始网格最高点与平滑图最高点可能不同，不能混用。

运行目录 results/stage24_evidence/20260912_z。当前开始扫描 macaroni2 的 810 张正常训练图，之后处理 capsules。已有模型、记忆库和训练数据均不修改。

来源恢复不证明对应错误。完成后需要结合查询和参考的可视化，区分正常变化、缺陷证据不足、背景响应或真正错误对应；没有证据时不据此增添模块。
