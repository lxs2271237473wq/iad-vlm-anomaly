# 温和干预扩大复核

运行目录：results/stage24_evidence/20260912_r。固定源域 VisA 每类按 image_id 取前 16 张正常和前 16 张异常，12 类共 384 张。原图、亮度 ×0.9/1.1、对比度 ×0.9/1.1，共 1920 个视图。子集包含上一轮样本，属于探索性扩展，不是独立复现。

冻结 512 PatchCore 及源类别留一 MLP，头部种子 42。重新提取真实干预图的异常图及外观特征。原图须与历史检测分数、特征及 MLP 分数一致。保留所有样本及饱和统计，不根据预测结果筛选图像。±10% 也不保证缺陷可见性保持，解释时仍须核查。

主诊断量为：MLP 干预前后 AUROC 差值减去检测器干预前后 AUROC 差值。负数代表 MLP 额外退化。报告全部四种变换，不选最差一种作为普遍结论。

按类别内正常/异常图像分别配对重采样 5000 次，同一图像在模型和干预间共用权重；不把图像配对当成独立样本。区间条件于固定类别、样本和头部，未处理未知场景相关性、多轮探索及多重比较，不用于宣称确认性显著结论。

推理结束自动生成逐类差值与宏平均差值区间。先检查错误是否仍集中在少数类别，再决定是否值得开展训练。不会因任一单项下降就自动启动一致性训练；若没有一致的额外退化证据，停止这一机制路线。

本轮不读取 AD2，不训练或更新模型，不构成新方法成绩。
