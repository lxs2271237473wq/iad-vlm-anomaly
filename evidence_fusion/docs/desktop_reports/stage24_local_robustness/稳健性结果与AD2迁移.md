# 稳健性结果与 AD2 迁移

源域稳健性队列退出码 0，12 折完成，所有线性优化均成功，最大梯度约 4.1e-8～1.52e-7，满足事前阈值 1e-5。

## 三种子结果

| 输入 | 宏平均 AUROC 的三种子均值 | 种子标准差 | 范围 |
|---|---:|---:|---:|
| 仅异常响应 | 0.964925 | 0 | 0.964925～0.964925 |
| 四项异常图统计 | 0.966605 | 0.000052 | 0.966558～0.966660 |
| 完整七特征 | **0.983581** | **0.000732** | **0.983151～0.984427** |

原 512 检测器为 0.965426；新凸优化线性控制为 0.976511，旧 AdamW 线性头为 0.977760。凸优化训练目标收敛并不保证更高评估 AUROC；它与旧 AdamW 目标的正则化也不完全相同。

## 同监督控制

| 种子 | 完整特征 − 收敛线性 | 95% 区间 | 完整特征 − 异常图统计 | 95% 区间 |
|---|---:|---:|---:|---:|
| 42 | +0.007915 | [+0.004478, +0.011504] | +0.017868 | [+0.012376, +0.023715] |
| 123 | +0.006654 | [+0.003437, +0.009959] | +0.016569 | [+0.011109, +0.022484] |
| 2026 | +0.006640 | [+0.003463, +0.010011] | +0.016491 | [+0.011146, +0.022296] |

三个种子均优于上述控制，支持继续验证完整局部信息的迁移价值。完整特征多出的灰度均值、灰度标准差和梯度作为一个整体有增量信息；尚未分离其中每个特征的贡献，不能将收益全归因于某一种纹理机制。

本实验只改变头部初始化和批次顺序，骨干、内存库及像素采样固定；不代表全流程多种子。区间条件于固定预测，未校正历次探索或本轮多重比较。各模型使用源域像素监督，不是官方纯正常训练协议。名义 MLP 架构相同，置零输入的有效自由度不同。

## 已启动目标迁移

服务器目录 `results/stage24_evidence/20260911_l`，队列 PID 14227。

使用全部源类别已有像素样本，重新拟合源域标准化器与最终头部；固定三种子 42、123、2026，全部报告，不选择最优种子。保持三个输入变体和收敛线性控制。输入特征迁移实现首先与原源域实现进行逐元素一致性检查。

AD2 仅使用已有正常内存库和正常校准参数；学习器不使用 AD2 标签、掩码或异常样本拟合。1084 张目标图的全部无标签预测保存后，才加载目标评估标签，输出八类 AUROC/AP、三种子范围及 5000 次临时场景分组区间。没有根据目标结果调权重或重新选择特征。

AD2 公开结果已被此前多轮查看，必须披露，不能当成完全未接触的盲测。目标泛化仍未知；源域正向结果也不构成普通 MLP 的算法新颖性证明。

当前源域验证完成，AD2 新学习器迁移在运行。`queue.log`、`queue.exit`、`frozen_config.json`、`complete.json` 用于检查状态；本次未设置定时通知。
