# 项目整理完成说明

整理对象为 SSH 服务器上的 `/root/private_data/iad-vlm-anomaly`，本机工作区不是该仓库的完整检出。未启动训练、未删除数据、未提交或推送 Git。

## 总目录入口

```text
iad-vlm-anomaly/
├── README.md                 总导航
├── datasets/                 公共数据
├── models/                   通用预训练模型
├── configs/                  通用基线配置
├── knowledge/                领域知识
├── third_party/              公共依赖
├── downloads/、backups/       下载与备份
├── srb_qcr/                  早期 AD、VLM、QCR/SRB、Stage7–23
├── evidence_fusion/           Stage24 表征、分辨率、参考与融合
├── tiny_defects/              Stage25 微小缺陷专项
├── orbitad/                   A0–A11 正常变化与局部检测探索
├── ad2_model_zoo/             外部 AD2 方法与权重，暂停状态
├── model_comparison/          历史成绩、FP/FN、模型互补审计
└── maintenance/organization_20260915/  迁移与回滚
```

## 每条实验主线

新增主线统一提供 `src/`、`configs/`、`docs/`、`results/`、`runs/`、`logs/`、`checkpoints/`。旧权重保留在原运行记录的 weights 子目录，避免重复复制。OrbitAD 和模型库原有内部结构保留。

SRB、Stage24、Stage25、OrbitAD 均增加 `STUDIES.md` 和 `studies/<阶段>/`，集中链接到该阶段代码及结果。日期阶段的代码匹配来自脚本中已有运行标识，是导航辅助，不等同完整依赖声明。Stage25 复用 Stage24 的公共运行代码，通过 `tiny_defects/src/stage24_runtime` 访问，不复制出两套实现。

## 改动与验证

- 原地移动131项目录/归档，移动前后 inode 和设备号一致；对应旧路径均保留兼容链接。
- 14个脚本仅修改项目根目录定位表达式，备份原文并记录前后哈希；通过 Python AST 语法检查。
- 首轮336个阶段链接无断链，环境Python入口可运行。后续完整链接检查单独记录在服务器 `symlink_scan.json`。
- 导入91份本机文档/分析文件，包括重算FP/FN和模型互补表所需数据。
- 补齐新目录Git忽略规则，防止环境、模型、缓存及大型产物意外加入版本控制。
- 没有重跑全部历史实验；这次验证范围是文件组织、路径兼容和语法。

旧 `experiments/`、`results/`、`runs/`、`docs/`、`logs/` 中仍可见兼容链接，并保留公共基线实体。不要把这些链接误当作重复数据删除。浏览优先用新实验入口，运行历史脚本仍从总目录执行。

## 回滚

服务器执行：

```bash
cd /root/private_data/iad-vlm-anomaly
/opt/conda/bin/python maintenance/organization_20260915/rollback.py
```

它恢复迁移实体、14份原代码与原Git忽略规则，不清理新生成的导航/文档；回滚后导航可能指向新布局，需以原始目录为准。若整理后又手动移动了文件，脚本会拒绝覆盖，不强制删除。
