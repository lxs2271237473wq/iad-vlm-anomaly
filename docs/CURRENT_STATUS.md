# 当前实验状态（2026-09-20）

## 结论

实验尚未达到可投稿定稿状态。A69/A70 的整体数值应保留为探索记录，不能作为正确的正常校准 448/672 融合证据。

## 已确认的协议错误

- A45 日志确认七个 AD2 类别的参考与全局分辨率为 672，Sheet Metal 为 448。
- A67 全类别的参考与全局分辨率为 448，局部分支为 672。
- A69 的七类全局测试图读取 A45，而正常 `global_q99` 读取 A67 并经 A68 汇总。
- 因此七类实际执行的是“672 协议的 `G` 除以 448 协议的 `qG`”。A66/A70 的 `global_448` 字段名称也不准确。

原始结果应保持不变。修复必须新增实验版本，补齐与 A67 完全一致的测试组件图，或者重新生成匹配 A45 的正常校准图并重新定义方法。仅修改显示标签不能视为修复。

## 目前可以保留的证据

- A72 Cable 的全局 448、局部 672 配置一致，可作为初步正结果。
- Cable 的正常校准图来自 `train/good`，但校准池与参考选择池有复用，并且 Cable 已被前序实验观察，因此不是严格盲测。
- A73 的生成脚本明确采用参考/全局 448、局部 672，可用于同协议跨类验证；只有完整评估和来源检查完成后才能进入论文主表。
- A69 的 `0.602419` 只能作为开发搜索记录，不能作为修复后方法成绩。

## 下一轮验收条件

1. 统一权重、参考图清单、预处理、参考分辨率和全局/局部分辨率，并保存配置指纹。
2. 先划分独立正常校准池，再进行参考选择，避免数据角色重叠。
3. 增加未经校准的简单均值、单分支、同预算分辨率和可靠外部基线。
4. 完成 MVTec 多类别以及最终未参与方法选择的测试。
5. 报告类别宏平均、实例分组重采样置信区间、固定阈值 FP/FN、运行时间与显存。
6. 若校准增益不稳定，应把贡献收缩为跨分辨率互补分析，不能强行宣称稳定降低误报。

## 论文证据边界

当前实现是冻结编码器、非参数记忆库和离线正常校准，没有可训练路由器、像素自适应门控或 VLM 在线判别。q99 是正常分数尺度，不是异常概率或直接的 FPR 阈值。GT 只应用于评测。论文流程图和方法描述必须与这些事实一致。
