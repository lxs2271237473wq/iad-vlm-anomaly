# 路线切换：更换表示，不继续修补裁剪头

暂停旧 WRN50 七维统计、外观近邻、上下文对应及当前局部裁剪修补。保留基线与数据划分，转向 DINOv2 深层 patch 表示。用户要求面向实际成果，后续每次方向选择必须对应完整图像检测结果，不以诊断数量代替方法进展。

已部署 facebook/dinov2-base，518 输入，1369 个 patch token，L2 归一化后与正常参考做余弦近邻。每类固定前 32 张正常训练图，保留全部参考 token；不压成之前的七维统计。主评分为最大 patch 距离，补充固定最高 1% 均值，两个都报告，不按目标结果挑选。

首轮评估全部 1462 张 source_fit 图，报告全量以及 tiny_only 加全部正常图的 AUROC/AP、逐类和配对区间。验证、开发留出和 AD2 不进入本轮。正常参考数量与原检测器不同，需要在结果解释中披露。

[AnomalyDINO 官方实现](https://github.com/dammsi/AnomalyDINO) 已使用 DINOv2 做 patch 异常检测，因此这是新表示基线，不是新算法，也不是官方完整复现。若有效，再以它为强控制开发有独立机制的新方法；不得把换骨干包装成创新。

推进规则：一轮完整基线后决策。若微小专项和全量均明显落后原检测器，除实现错误或明确协议问题外不做零碎参数搜索；若源训练结果有改善，先固定配置进入源验证，仍不能直接宣布迁移成功。新模块必须超过新基线及同监督简单头，并接受正常误报和推理成本约束。

若这一表示也不支持微小缺陷方向，应重新评估问题选择，而不是保证任意方向一定能出论文。发现不支持预期的结果必须如实保留；不得筛掉类别、改测试标签或反复使用 AD2 选择最佳方案。

当前权重加载与基线任务已启动，没有新性能结果。服务器日志 results/stage24_evidence/stage25_dinov2.log；结果目录 results/stage25_tiny_defects/dinov2_baseline_v1。
